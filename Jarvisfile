- stage: Prepare (install pipenv)
  script: pipenv --bare install --dev
- stage: Test (execute pytest)
  script: pipenv run pytest
- stage: Test (execute pytest)
  script: pipenv run pyinstaller jarvis.spec
